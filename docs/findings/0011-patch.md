## Amendment — 2026-09-04

The logged alternative below was recorded partly as an escape route from a
defect reported in Finding 003: that `type meter` lost the final frames of a
stream.

**Finding 004 withdrew that.** The same artefact occurs with the plugin removed,
so it belongs to `snd-aloop` and is a property of the measurement rig, not of
the audio chain. The metering path has no known defect.

The alternative therefore loses that motivation and now stands or falls on its
other merits — freedom from peppyalsa's `decay_ms`, smoothing and 0-100 scale.
Those are real but were never the reason it was filed. Its priority drops
accordingly.
